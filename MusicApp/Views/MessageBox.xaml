<Window x:Class="MusicApp.Views.MessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:MusicApp.Views"
        mc:Ignorable="d"
        WindowStyle="None"
        Background="{x:Null}"
        WindowStartupLocation="CenterScreen"
        Title="MessageBox" Height="250" Width="400" ResizeMode="NoResize">

    <Grid>
        <!--<materialDesign:Card Margin="10" UniformCornerRadius="20" Background="{x:Null}">
            <StackPanel Background="#C4F3FF">
                <Grid>
                    <Button Background="{x:Null}" Style="{StaticResource btnNavi}" HorizontalAlignment="Right" ToolTip="Close App" x:Name="btnClose" Click="btnClose_Click">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Assets/Images/icon_exit.png" Height="59" Width="59" />
                        </StackPanel>
                    </Button>
                </Grid>
            </StackPanel>-->
        <!--</materialDesign:Card>-->
    </Grid>
</Window>
